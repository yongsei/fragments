# SPA fallback for React Router
/*    /index.html   200

# API routes (if you have any)
/api/*  /api/:splat  200

# Static files (already handled by Netlify, but good to be explicit)
/static/*  /static/:splat  200